# Dependabot 設定。5 つの uv プロジェクト + GitHub Actions を対象。
# minor/patch は per-component でグルーピングし PR 数を抑える。
# 生成された PR は dependency-triage ワークフローが判定し、安全なものは自動マージ。
version: 2

updates:
  # ---- Python (uv) 各コンポーネント ----
  - package-ecosystem: "uv"
    directory: "/agent"
    schedule: { interval: "weekly" }
    labels: ["deps", "area:agent"]
    open-pull-requests-limit: 5
    commit-message: { prefix: "deps(agent)" }
    # pyproject で major をピン中。移行 PR が入るまで major の再提案を止める。
    # a2a-sdk 1.x はカード wire format 非互換（agent/pyproject.toml のピン参照）
    ignore:
      - dependency-name: "a2a-sdk"
        update-types: ["version-update:semver-major"]
    groups:
      agent-minor-patch:
        update-types: ["minor", "patch"]

  - package-ecosystem: "uv"
    directory: "/mcp-server"
    schedule: { interval: "weekly" }
    labels: ["deps", "area:mcp"]
    open-pull-requests-limit: 5
    commit-message: { prefix: "deps(mcp-server)" }
    # pyproject で major をピン中。移行 PR が入るまで major の再提案を止める。
    # mcp 2.x は main.py の移行が必要（mcp-server/pyproject.toml のピン参照）
    ignore:
      - dependency-name: "mcp"
        update-types: ["version-update:semver-major"]
    groups:
      mcp-minor-patch:
        update-types: ["minor", "patch"]

  - package-ecosystem: "uv"
    directory: "/bff"
    schedule: { interval: "weekly" }
    labels: ["deps", "area:bff"]
    open-pull-requests-limit: 5
    commit-message: { prefix: "deps(bff)" }
    groups:
      bff-minor-patch:
        update-types: ["minor", "patch"]

  - package-ecosystem: "uv"
    directory: "/cli"
    schedule: { interval: "weekly" }
    labels: ["deps", "area:cli"]
    open-pull-requests-limit: 5
    commit-message: { prefix: "deps(cli)" }
    groups:
      cli-minor-patch:
        update-types: ["minor", "patch"]

  - package-ecosystem: "uv"
    directory: "/tests/integration"
    schedule: { interval: "weekly" }
    labels: ["deps"]
    open-pull-requests-limit: 5
    commit-message: { prefix: "deps(itest)" }
    groups:
      itest-minor-patch:
        update-types: ["minor", "patch"]

  # ---- GitHub Actions ----
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule: { interval: "weekly" }
    labels: ["deps", "automation"]
    open-pull-requests-limit: 5
    commit-message: { prefix: "deps(actions)" }
    groups:
      actions-all:
        patterns: ["*"]
