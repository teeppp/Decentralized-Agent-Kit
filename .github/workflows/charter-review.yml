name: Charter review

# 四半期: 目的憲章(docs/CHARTER.md)そのものを見直す Issue を起票（要件4の「目的の定期見直し」）。
# provider 中立（MAINT_LLM_*）+ Web 検索（Tavily API, TAVILY_API_KEY）。
# MAINT_LLM_* / TAVILY_API_KEY 未設定なら 0 件（失敗しない）。規約遵守のためスクレイピングはしない。
on:
  schedule:
    - cron: "0 7 1 1,4,7,10 *" # 1/4/7/10 月の 1 日 07:00 UTC（四半期）
  workflow_dispatch:

permissions:
  contents: read
  # MAINT_LLM_MODEL=bedrock/...: assume the Bedrock role over OIDC (IAM, no API key).
  id-token: write
  issues: write

jobs:
  charter-review:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      MAINT_LLM_BASE_URL: ${{ vars.MAINT_LLM_BASE_URL }}
      MAINT_LLM_MODEL: ${{ vars.MAINT_LLM_MODEL }}
      MAINT_LLM_API_KEY: ${{ secrets.MAINT_LLM_API_KEY }}
      TAVILY_API_KEY: ${{ secrets.TAVILY_API_KEY }}
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-dependency-glob: "maintenance/uv.lock"
      - name: AWS credentials for Bedrock (OIDC)
        id: aws
        if: ${{ vars.MAINT_AWS_ROLE_ARN != '' }}
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ vars.MAINT_AWS_ROLE_ARN }}
          aws-region: ${{ vars.MAINT_AWS_REGION || 'us-east-1' }}
          # Only the step that calls the LLM gets them (not uv sync, not issue creation).
          output-credentials: true
          output-env-credentials: false

      - name: Generate charter-review issue
        working-directory: maintenance
        env:
          AWS_ACCESS_KEY_ID: ${{ steps.aws.outputs.aws-access-key-id }}
          AWS_SECRET_ACCESS_KEY: ${{ steps.aws.outputs.aws-secret-access-key }}
          AWS_SESSION_TOKEN: ${{ steps.aws.outputs.aws-session-token }}
          AWS_REGION: ${{ vars.MAINT_AWS_REGION || 'us-east-1' }}
        run: |
          uv sync
          quarter="$(date -u +%Y)-Q$((($(date -u +%-m)-1)/3+1))"
          existing=$(gh issue list --label charter --state open --json title --jq '[.[].title | sub("^要望: *"; "")]')  # 起票時に付ける「要望: 」を外して比べる
          uv run dak-maint charter-review \
            --charter ../docs/CHARTER.md \
            --quarter "$quarter" \
            --existing-titles "$existing" > proposals.json
          cat proposals.json

      - name: Create request issues
        working-directory: maintenance
        env:
          # GITHUB_TOKEN で作った Issue は project-autoadd を起動しないので、
          # request_issue.py が Project への追加と Status=Backlog を自分で行う。
          DAK_PROJECT_URL: ${{ vars.DAK_PROJECT_URL }}
          DAK_PROJECT_TOKEN: ${{ secrets.DAK_PROJECT_TOKEN }}
        run: python3 ../scripts/setup/request_issue.py proposals.json --source charter-review
