[project]
name = "mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.28.1",
    "mcp>=1.28.1",
    "uvicorn>=0.51.0",
    "starlette>=1.3.1"
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
